{
  "kind": "collectionType",
  "collectionName": "projects",
  "info": {
    "singularName": "project",
    "pluralName": "projects",
    "displayName": "Projects",
    "description": "Manage Projects adding title, subtitle and image ( optional )"
  },
  "options": {
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "minLength": 5,
      "maxLength": 100
    },
    "subtitle": {
      "type": "text",
      "required": true,
      "minLength": 5,
      "maxLength": 250
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": false
    }
  }
}
